# 更新日志

本文档记录「转录组分析软件 TVAS」的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [0.1.16] - 2026-08-20

### 新增
- **安装日志系统（持久化到文件）**：
  - 每次安装自动创建带时间戳的日志文件，保存到 `~/.local/share/TVAS/logs/install/`
  - 日志头部记录时间、模式（全部安装/重装选中/自定义安装）、环境名、Conda 路径
  - 每个软件包单独记录：conda 完整命令、退出码、成功/失败、完整 stdout/stderr
  - 安装过程中边写边刷新，安装进行中即可查看已完成的输出
- **「更多操作 → 查看安装日志」**：
  - 日志查看对话框：下拉切换历史日志（按时间倒序）、等宽字体深色背景显示、刷新按钮
  - 修复之前弹窗提示"查看日志"但日志文件根本不存在的问题

### 优化
- **「更多操作」菜单布局重组**：
  - 按 日志（查看/打开文件夹）→ 安装（重装选中/刷新版本）→ 环境（系统终端）→ 危险操作（卸载选中/卸载全部）四组分区
  - 日志功能置顶，卸载等危险操作沉底并集中放置，降低误触风险
- **安装/重装失败弹窗改进**：
  - 直接列出失败的软件包名称和日志文件完整路径
- **安装进行中仍可查看日志**：忙碌状态只禁用操作类菜单项，日志查看保持可用

---

## [0.1.15] - 2026-08-20

### 修复
- **解决 Trinity 2.11.0 与新版 samtools 的兼容问题**：
  - 定位根因：旧版 Trinity（≤2.13）的 samtools 版本探测代码有缺陷，会把自带的插件目录加入 PATH 后误读 samtools 版本输出，导致"samtools 已装且达标却报缺失"（如本例 samtools 1.21 被误判）
  - 环境安装清单将 Trinity 从 2.8 改为 2.15（新装环境直接获得修复版）
- **Trinity 步骤增加自动诊断**：
  - 组装前检测 Trinity 版本，若 ≤2.13 给出升级预警和具体命令
  - 组装失败时若检测到"samtools 明明通过预检查却报缺失"，自动输出诊断说明和升级命令，不再让用户误以为 samtools 有问题

---

## [0.1.14] - 2026-08-20

### 优化
- **失败日志不再截断关键信息**：
  - 各步骤失败时的输出捕获从 200-300 字符增加到 600 字符
  - Trinity 失败时同时显示错误输出的头部（首个错误）和尾部（最终报错），修复之前错误开头被截断成 "e/TVAS/..." 导致无法定位原因的问题

---

## [0.1.13] - 2026-08-19

### 优化
- **Trinity 前置依赖预检查（快速失败）**：
  - Trinity 组装开始前先检测 samtools 是否安装且版本 ≥1.3，缺失/过旧立即中止并给出具体补装命令
  - 避免先做十几分钟的输入文件完整性校验、到组装启动时才报 samtools 错误

---

## [0.1.12] - 2026-08-17

### 修复
- **补装 samtools（Trinity 运行时依赖）**：
  - 环境安装清单新增 samtools（不锁版本，保证满足 Trinity 要求的 ≥1.3）
  - 修复 Trinity 组装启动即失败的问题：`Error, need samtools installed that is at least as new as version 1.3`
  - 已建好的环境无需重建：到「环境设置」页重新点击安装即可补装缺失的 samtools

---

## [0.1.11] - 2026-08-17

### 修复
- **续跑时清除全部下游过期输出**：
  - 从中间步骤续跑时，不再只清除"最后一个有输出"步骤的目录，而是清除所有将要运行步骤的已有输出
  - 避免下游步骤残留的旧文件（基于错误上游数据生成或中途停止留下）混入新结果
- **删除操作移至确认之后**：
  - 修复续跑确认弹窗弹出前就已删除步骤输出的问题；现在点击「是」之后才执行删除，点「否」不再丢失任何数据
  - 确认弹窗中新增「将清除的过期输出」清单，明确列出将被删除的文件夹

---

## [0.1.10] - 2026-08-17

### 修复
- **截断文件检测（防止坏数据流入 Trinity）**：
  - Trinity 组装前对全部输入执行 `gzip -t` 完整性校验，可检出上游进程被超时/停止杀死后留下的截断 gz 文件（此类文件大小非零但内容不完整，肉眼无法分辨）
  - 输入文件为空（0 字节）也会被拦截并列出清单
- **样本级续跑（替代目录级跳过）**：
  - Fastp / Rcorrector 不再"目录非空就整步跳过"，改为逐样本校验输出：文件存在且非空（Rcorrector 额外做 gzip 完整性校验）才跳过该样本
  - 上次运行中断留下的空文件/截断文件会被自动识别并仅重跑受影响的样本，不必整步重来

---

## [0.1.9] - 2026-08-14

### 修复
- **单样本失败不再被静默忽略（核心修复）**：
  - 修复 FastQC / Fastp / Rcorrector 步骤中单个样本失败（命令失败、超时、输入文件不存在）只记日志、步骤仍显示"成功"的问题
  - 现在任何样本失败都会将步骤标记为失败并列出失败样本名，流程立即终止，不再把问题拖到下游
  - 修复因上游单样本失败导致 Trinity samples_file 引用不存在文件、组装崩溃报错晦涩的问题
- **Rcorrector 输出校验**：
  - Rcorrector 命令执行成功后额外校验输出文件（`*_R1_clean.cor.fq.gz`）确实生成，防止静默失败
- **Trinity 输入预检查**：
  - Trinity 组装前校验 samples_file 中所有输入文件存在，缺失则中止并列出缺失文件清单，给出明确提示

### 优化
- **超时时间放宽**（适应大 fastq 文件）：
  - FastQC: 10 分钟 → 1 小时
  - Fastp: 20 分钟 → 4 小时
  - Rcorrector: 30 分钟 → 24 小时

---

## [0.1.8] - 2026-08-14

### 修复
- **面包屑导航 NameError**：
  - 修复运行打包版本时 `_update_breadcrumb` 方法抛出 `NameError: name 'module_names' is not defined` 导致程序无法启动的问题
  - 该变量在 V0.1.6 重构时已替换为全局常量 `MODULE_NAMES`，但漏改了 `_update_breadcrumb` 中的一处引用
  - 现已统一使用 `MODULE_NAMES[0]`

---

## [0.1.7] - 2026-08-14

### 精简
- **删除冗余文件**：
  - 删除 `debian/` 目录（Debian 打包规范文件，build.sh 用 heredoc 自动生成，未使用）
  - 删除 `uos/` 目录（UOS 商店上架材料，build.sh 未引用）
  - 删除 `rnaseq_app/resources/rnaseq-denovo.desktop`（build.sh 用 heredoc 自动生成）
  - 删除 `run.sh`（功能与 README 中的 `python3 main.py` 重复）
- **更新 README.md**：移除对已删文件的引用，更新项目结构说明

## [0.1.6] - 2026-08-14

### 新增
- **导出日志功能**：
  - 运行日志区新增「导出日志」按钮（位于清空按钮右侧）
  - 支持导出为 TXT（纯文本）和 Markdown（带标题和代码块包裹）两种格式
  - 导出文件名自动带时间戳，如 `tvas_log_20260814_153000.txt`

### 优化
- **GroupBox 标题间距优化**：
  - 减小「执行步骤选择」和「运行日志」标题与内容之间的间距
  - 运行日志区可显示更多行内容
- **代码精简**：
  - 删除未使用的 `import subprocess` 和 `QSplitter` 导入
  - 统一 `module_names` 列表为模块级常量 `MODULE_NAMES`，消除重复硬编码

## [0.1.5] - 2026-08-14

### 新增
- **续跑预扫描与确认**：
  - 点击续跑时自动扫描工作目录中各步骤的输出完成状态
  - 弹窗显示：已完成步骤（将跳过）和未完成步骤（将重新运行）
  - 最后一个有输出的步骤可能因中途停止而未完成，自动清除其输出并重新运行
  - 续跑确认弹窗分三类展示：✅ 已完成（跳过）、🔄 可能未完成（已清除重跑）、▶ 未运行（将执行）
- **从头运行清空确认**：
  - 点击从头运行时，自动检测工作目录中已有的分析文件夹
  - 弹窗列出将删除的文件夹，确认后清空再从第一步开始
  - 避免旧结果干扰新运行

### 优化
- **样本配置表格自适应大小**：
  - 表格区域随窗口大小自动伸缩，窗口放大时表格同步放大
  - 移除底部空白拉伸，配置区填满整个页面
- **执行步骤选择区域重做**：
  - 必需步骤标 ★，可选步骤标 ○，使用网格布局对齐
  - 提示文字说明标记含义和运行状态类型
  - 每步显示功能描述，状态标签紧挨步骤名（不再放到最右侧）
  - 步骤选择区限制最大高度，不再挤压运行日志区
- **任务运行状态显示优化**：
  - 状态从难以理解的符号改为文字标签：等待中 / 运行中… / 已完成 / 失败 / 已跳过
  - 运行中的步骤用蓝色背景高亮，已完成的用绿色背景，失败的用红色背景
  - 状态标签带圆角背景色，紧邻步骤名，一眼可辨当前运行到哪一步
- **Tab 标签样式优化**：
  - Tab 标签增加圆角边框和背景色，选中项白色背景 + 主题色下划线
  - 未选中项添加 hover 效果，视觉层次更清晰
  - 设置最小宽度，避免标签文字被截断
- **全局界面布局优化**：
  - 窗口默认尺寸调整为 1280×850，最小高度增至 750
  - 面包屑导航栏加大内边距
  - 进度条加高至 28px，字体加粗

### 修复
- **续跑逻辑修复**：
  - 修复续跑时最后一步有输出但可能未完成时仍被跳过的问题
  - 现在续跑会自动识别最后一个有输出的步骤，清除其输出并重新运行
  - 例如：运行步骤1中途停止后续跑，从步骤1重新开始（而非步骤2）
  - 例如：运行步骤5中途停止后续跑，自动清除步骤5输出并重新运行步骤5

---

## [0.1.4] - 2026-08-14

### 新增
- **停止/暂停功能修复（核心改进）**：
  - 改用 `subprocess.Popen` + 进程组管理，点击「停止」可立即终止当前运行的命令
  - 步骤函数在样本循环中增加取消检查，不再需要等待整个步骤完成
  - 停止后已完成的步骤结果保留，可点击「续跑」从中断处继续
- **续跑模式（断点续跑）**：
  - 新增「⚡ 续跑」按钮，自动检测每步输出是否已存在，跳过已完成的步骤
  - 续跑模式下从上次中断处继续运行，避免重复计算
  - 每步输出保存在独立子文件夹（01_fastqc_out, 02_fastp_clean 等），便于管理
- **步骤可跳过（用户自定义流程）**：
  - 步骤 1/2/3（FastQC/Fastp/Rcorrector）标记为可选，可取消勾选跳过
  - 步骤 7/8/9/10/11（重命名/TransDecoder/Gffread）标记为可选，可跳过
  - Trinity 智能选择输入源：根据上游实际运行的步骤自动选用 rcorrector/fastp/原始fq
  - 必需步骤（Trinity/最长Isoform/CD-HIT）不可取消勾选

### 优化
- **界面布局调整**：
  - 移除左侧步骤导航栏（在前三步配置阶段无实际用途）
  - 将执行步骤选择与运行状态显示整合到「4. 任务运行」页
  - 运行前：复选框选择步骤；运行中：实时显示每步状态（○待运行/◉运行中/✓完成/✗失败/−跳过）
  - 必需步骤标记为不可取消，可选步骤标注「(可选)」
- **内存参数交互优化**：
  - Trinity 最大内存从文本输入改为数值 SpinBox + 固定「G」单位后缀
  - 用户只需调整数字，无需手动输入单位
  - 默认值提示同步更新为「默认 50G」

---

## [0.1.3] - 2026-08-13

### 优化
- **界面导航重构（清晰展示当前步骤）**：
  - 顶部模块导航标签改为中文编号：「一、de novo 组装」「二、序列比对」「三、差异表达分析」
  - 新增面包屑导航栏，实时显示当前所处的大步骤（模块）与小步骤（Tab），用户不再迷失
- **de novo 组装流程顺序调整**：
  - 调整为「环境设置 → 样本配置 → 参数配置 → 任务运行」四步
  - 将原「参数配置」中的执行步骤选择移至新增的「4. 任务运行」页
  - 将底部运行日志转移至「4. 任务运行」页，避免在环境/样本配置阶段显示无关日志，界面更清爽
- **参数配置页交互优化**：
  - 数值参数滚轮仅在控件获得焦点时才修改数值，滑动页面不再误改参数
  - 为每个数值参数添加默认值提示（如「默认为 5」），缩短编辑条长度，布局更美观
  - 新增「↺ 恢复默认参数」按钮，一键将所有参数恢复为默认值
  - 运行时仅禁用配置页，保留「任务运行」页可操作（查看日志、停止）

---

## [0.1.2] - 2026-08-13

### 优化
- **样本配置页重构（de novo 组装简化）**：
  - 去掉手动输入「条件/分组 | 重复名」列，改为 3 列：样本名 | R1 路径 | R2 路径
  - 点击「📂 选择 FASTQ 文件」多选文件，系统按文件名 `_R1`/`_R2`（或 `_1`/`_2`）自动配对填表
  - 样本名自动从文件名推导（可手动修改），R1/R2 路径只读防止误改
  - 无法配对的文件给出提示并忽略，引导用户检查文件名后缀
  - 新增提示「⚠ 暂时仅支持双端测序数据」
  - de novo 组装 group/replicate 统一取样本名，Trinity samples_file 自动生成
- **参数配置页显示优化**：
  - 整页纳入 QScrollArea 滚动区，窗口较小时也能完整浏览所有参数
  - 提示文字字号从 11px 调大到 12px，颜色加深（#7f8c8d），并启用自动换行
  - 每个输入框增加 tooltip 说明参数含义和取值建议
  - 表单行距加大（spacing 12px），内边距加宽，标签右对齐更整齐
  - Trinity/CD-HIT 新增参数取值范围说明提示行
- **软件包安装模块精简**：
  - 移除底部的「查看选中包日志」「查看最近命令输出」两个不常用按钮
  - 同步清理 env_manager 中的 `last_log`/`last_cmd`/`pkg_logs`/`get_package_log` 等相关代码

---

## [0.1.1] - 2026-08-13

### 修复
- **修复点击「刷新已安装版本」闪退问题**：
  - 根因：`_refresh_versions_from_env` 的 `task_fn` 把结果列表作为元组第二项返回，
    被 `EnvTaskWorker.run()` 统一 `str()` 化后，`on_done` 中 `for row, ver in results:`
    在字符串上解包引发 `ValueError: not enough values to unpack`，异常逃逸到 Qt 信号
    回调导致进程崩溃
  - 修复：与其他 conda 任务（安装/验证/卸载）保持一致 — 结果存到实例属性
    `self._refresh_results`，`task_fn` 只回传字符串摘要；`on_done` 从实例属性读取，
    并对 `pkg_table.item(...)` 做空值保护
  - 顺便对版本单元格补 `None` 判断，避免空表格行再次触发 AttributeError

---

## [0.1.0] - 2026-08-07

### 变更
- **开源协议从 MIT 更改为 GPL v3**：与 PyQt5 依赖的协议一致，合规开源发布
- **环境终端改用 UOS 系统终端**：点击「打开系统终端」按钮启动 deepin-terminal，
  自动 conda activate 进入分析环境 + cd 到工作目录
  - 弃用自建终端（QTermWidget/pty），原生体验无卡顿
  - 修复命令执行后误触发版本刷新的 bug
- **工作目录设置加说明**：参数页标注输出到各子文件夹（01_fastqc_out 等）
- 版本号从 0.0.x 迈入 0.1.0 — 首个公开发布版本

---

## [0.0.19] - 2026-08-07

### 新增
- **终端 Tab 键自动补全**（验证软件安装更方便）：
  - 命令补全：`fast<Tab>` → fastqc / fastp
  - conda 子命令补全：`conda i<Tab>` → install / info / init / inspect
  - 文件/目录补全：`ls ~/<Tab>` 等
  - 唯一候选自动补全 + 空格；多候选补公共前缀并在终端显示候选列表
  - 命令运行中 Tab 不响应（避免干扰）
- 基于 bash 内置 `compgen`，在分析环境内实时查询，无额外依赖

---

## [0.0.18] - 2026-08-07

### 重构 (嵌入式 pty 真终端)
- **环境终端升级为基于 pty 伪终端的嵌入式小终端**：
  - 实时流式输出（不再等命令结束才显示，可看到安装/下载中间过程）
  - 执行中可继续输入内容发送给进程（conda 询问 y/n 时直接输入 y 回车确认）
  - 上下键切换命令历史
- **终端 UI 视觉一体化**：输出区 + 输入行合并到一个黑底圆角容器内，
  不再是"上方显示窗口+下方输入框"的分离外观，整体看起来就像一个终端
- 新增 `TerminalWorker(QThread)` + `TermInput(QLineEdit)` 类
- 含 Windows 开发机 fallback（pty 不可用时退化一次性捕获）

### 设计决策
- **未采用"真终端控件"（QTermWidget）**：C++ 库打包与 PyQt5 Qt 实例冲突风险高，UOS Python 绑定不可靠
- **未采用"嵌入系统终端"**：QX11EmbedContainer 在现代桌面环境脆弱，打包后不可控
- **采用 pty 方案**：用 Python 内置 `pty` 模块 + QThread 实现，零外部依赖，零打包风险，体验达 90% 真终端

---

## [0.0.17] - 2026-08-07

### 修复 (重要)
- **所有 conda 操作改为后台线程执行**，界面不再冻结假死：
  - 验证安装 / 安装全部 / 重装选中 / 卸载选中 / 卸载全部 / 自定义安装 / 环境终端
- **任务执行期间明确忙碌状态**：
  - 鼠标变为等待光标（⏳）
  - 底部状态栏显示「⏳ 正在验证软件包，请稍候...」等提示
  - 所有操作按钮禁用（防止重复点击）
  - 完成后恢复按钮、状态栏回「就绪」
- 新增 `EnvTaskWorker(QThread)` 后台任务类，通过信号回传结果
- 按钮状态统一由 `_refresh_env_buttons()` 管理（依据环境是否就绪）

---

## [0.0.16] - 2026-08-07

### 改进
- **高级设置彻底收纳**：从「带复选框的折叠标题」改为「按钮点击展开」
  - 默认完全隐藏，看不到任何高级设置内容
  - 点击「▸ 高级设置（自定义安装 · 环境终端 · 命令日志）」按钮展开
  - 展开后按钮变「▾ 高级设置（点击收起）」
- **环境终端改为真终端交互**：
  - 移除「执行」按钮，回车即执行
  - 深色终端样式：黑色背景 + 绿色 `$` 提示符
  - 命令和输出追加到终端区（不覆盖历史），可连续输入多条命令
  - 滚到底部自动跟随新输出

---

## [0.0.15] - 2026-08-07

### 重构 (代码精简，功能不变)
- **main_window.py**：删除 13 个未使用 import；三处重复的 `_group_style` 合并为模块级函数；删除冗余的「运行当前步骤」按钮（与运行全部功能重复）；删除空 progress_callback
- **config.py**：删除未使用的 packages/output_structure/raw_data_dir 配置键，及 all()/get_output_dir()/packages()/raw_data_dir 方法
- **env_manager.py**：删除未使用的 run_script()/summarize()/is_required
- **steps.py**：删除顶层未使用的 import glob
- **pipeline.py**：删除未使用的 11 个 step_* 函数 import（步骤通过注册表调用）
- **删除冗余文件**：`scripts/ifdos2unix.py`（功能已内联到步骤中）、`install.sh`（已被 deb 包安装替代）
- 净减约 380 行代码

---

## [0.0.14] - 2026-08-07

### 改进
- **界面布局简化**：
  - 软件包管理主按钮行从 5 个精简为 3 个：「▶ 安装全部软件包」/「✓ 验证安装」/「更多操作 ▾」
  - 「更多操作 ▾」下拉菜单收纳：重装选中 / 卸载选中 / 卸载全部
  - 「高级设置」默认折叠成标题行，标题提示「自定义安装 · 环境终端 · 日志」
  - 主操作按钮加大（安装按钮用主题色突出），提高点击舒适度
  - 整体界面更简洁，常用功能一目了然

---

## [0.0.13] - 2026-08-07

### 新增
- **卸载选中软件包**：选中表格行（可多选）卸载，卸载后清空「已装版本」、状态恢复「未安装」
- **卸载全部（删除环境重建）**：直接删除整个分析环境（最干净，避免 conda 残留依赖），
  之后需重新「创建环境」并安装软件包；删除后表格自动清空、按钮状态复位
- env_manager 新增 `uninstall_package()` 和 `remove_env()`

---

## [0.0.12] - 2026-08-07

### 新增
- **环境终端**（高级设置）：在分析环境中手动执行任意命令
  - 支持 `conda install -c bioconda trinity=2.15`、`which Trinity`、`conda list` 等
  - 同步执行并显示完整输出（超时 2 小时，适合大软件包下载）
  - 执行成功后自动刷新表格中的已装版本
- **安装错误智能分析**：安装失败时自动识别常见错误并给出中文建议
  - UnsatisfiableError 依赖冲突 → 建议装新版/用 mamba/清缓存
  - PackagesNotFoundError → 建议去掉版本号
  - 网络错误 → 建议检查代理/配置国内镜像
  - 磁盘/权限错误 → 对应处理建议

---

## [0.0.11] - 2026-08-06

### 改进
- **布局优化**：环境设置页全部纳入滚动区，窗口小时仍能浏览；软件包表格最小高度 300，日志区最小 200
- **软件包表格新增「类型」列**：
  - 标题改为「软件包安装（★ 为 De Novo 流程必需）」
  - 表格列改为：软件包 / 已装版本 / 类型(★必需或可选) / 状态
  - ★ 必需 红色显示，可选灰色显示
- **版本列改为显示安装后实际版本**（不再预设），安装/重装/验证成功后自动查询 conda list 回填
- **自定义安装的包自动加入表格**：「自定义」蓝色显示，含版本和状态
- 每个 PackageSpec 新增 `required` 和 `description` 字段

---

## [0.0.10] - 2026-08-06

### 新增
- **高级设置面板**（环境设置页底部）：
  - 自定义软件包安装：输入包名（如 salmon / hisat2=2.2.1）自动安装到分析环境
  - conda 完整命令输出日志区（深色终端风格）
  - 「查看选中包日志」：查看指定软件包最近一次安装的完整输出
  - 「查看最近命令输出」：查看最近一次 conda 命令的完整日志
- 所有 conda 操作现在都会记录完整 stdout/stderr，便于定位安装失败原因

---

## [0.0.9] - 2026-08-06

### 修复
- 修复「重装选中软件包」闪退：Qt clicked 信号的 checked 参数被误当成表格项
- 按钮触发改用 lambda 包裹，内部用 isinstance 判断参数类型

---

## [0.0.8] - 2026-08-06

### 新增
- 「↻ 重装选中软件包」按钮：选中表格中的软件包即可单独重装（支持 Ctrl 多选）
- 双击软件包表格行也可快速触发重装

---

## [0.0.7] - 2026-08-06

### 修复
- 修复 CondaToSNonInteractiveError：新版 Miniconda 要求接受 Anaconda 服务条款
- 所有 conda 调用注入 `CONDA_PLUGINS_AUTO_ACCEPT_TOS=true`
- 本地 Miniconda 安装后自动执行 `conda tos accept`

---

## [0.0.6] - 2026-08-06

### 新增 (UI 架构升级)
- **三大分析模块导航**：顶部模块切换栏
  - 模块 1: De Novo 组装（已实现，11 步完整流程）
  - 模块 2: 测序数据比对（占位 — 计划 HISAT2/STAR 比对 + featureCounts 定量）
  - 模块 3: 基因差异表达分析（占位 — 计划 DESeq2/edgeR + GO/KEGG 富集）
- 新增 `ModuleNavBar`：顶部三模块切换条
- 新增 `ModulePlaceholderPage`：开发中模块占位页（含计划步骤说明卡片）
- 关于对话框更新为三大模块概览

---

## [0.0.5] - 2026-08-06

### 修复
- 修复 Qt xcb 平台插件缺失导致无法启动
- PyInstaller 增加 `--collect-all PyQt5` 完整打包 Qt 插件
- deb 增加 libxcb 系列系统依赖

---

## [0.0.4] - 2026-08-06

### 修复
- 修复 PyInstaller 打包缺少 `pkgutil` 模块导致启动崩溃 (ModuleNotFoundError)
- 显式打包 pkgutil/importlib 等标准库模块

---

## [0.0.3] - 2026-08-05

### 修复
- 改用 ubuntu-20.04 构建，兼容 UOS 的 glibc 版本

---

## [0.0.2] - 2026-08-04

### 新增
- **自包含 Conda 部署**：首次启动自动下载 Miniconda 到 `~/.local/share/TVAS/`，无需系统预装 Conda
- Conda 优先级：内置 > 本地 > 系统 > 自动下载
- 环境完全隔离，不影响 UOS 系统

### 变更
- 项目更名为 **TVAS**
- deb 包名 `TVAS_V0.0.2_amd64.deb`
- GitHub 仓库: `yangfty/TVAS`

---

## [0.0.1] - 2026-08-04 (内部开发)

### 新增
- 完整的 11 步转录组 de novo 组装分析流程
- PyQt5 图形化桌面界面（三栏布局）
- Conda 虚拟环境自动创建与管理
- 一键安装所有生物信息学软件 (FastQC, Fastp, Rcorrector, Trinity, Jellyfish, CD-HIT, TransDecoder, Gffread, Kallisto)
- 样本表格管理（手动添加 / samples_file 导入）
- 实时彩色日志输出 + 步骤状态指示灯 + 整体进度条
- 后台线程执行（不阻塞界面）
- 配置文件 JSON 保存/加载
- 可调参数：Fastp 质量阈值、CD-HIT 相似度、Trinity 内存、CPU 线程数
- PyInstaller 打包为独立可执行文件
- Debian (.deb) 打包支持
- UOS 应用商店上架规范兼容
- 应用图标（DNA 双螺旋主题）
- 桌面启动器 .desktop 文件
- 辅助脚本：序列重命名、GFF3 重命名、DOS/Unix 格式转换

### 分析步骤
1. FastQC — 原始数据质量评估
2. Fastp — 数据过滤与去接头
3. Rcorrector — RNA-seq reads 纠错
4. Trinity — de novo 转录本组装
5. 提取最长 Isoform — 每个基因保留最长转录本
6. CD-HIT — 聚类去冗余（默认 80% 相似度）
7. 重命名序列 — 规范化命名 + 换行符转换
8. TransDecoder LongOrfs — 识别长开放阅读框
9. TransDecoder Predict — 最终 CDS 预测
10. 重命名 GFF3 — 统一注释文件命名
11. Gffread — 提取 CDS / Protein 序列
